spring:
  application:
    name: saudacao-service

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"

saudacao-service:
  saudacao: "Ola (DEFAULT)"
  default-value: "Mundo (DEFAULT)"

