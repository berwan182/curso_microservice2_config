spring:
  application:
    name: saudacao-service

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"

saudacao-service:
  saudacao: "Ola (DEV)"
  default-value: "Mundo (DEV)"

