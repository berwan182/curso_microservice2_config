spring:
  application:
    name: saudacao-service

management:
  endpoints:
    web:
      exposure:
        include:
          - "*"

saudacao-service:
  saudacao: "Ola (PROD)"
  default-value: "Mundo (PROD)"

